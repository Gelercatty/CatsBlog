# CatsBlog

Hexo-based personal blog.

## Upload an image

Run from the repository root:

```bash
./scripts/upload-image --path path_to_img
```

The command uploads the image to the `Gelercatty/image-hosting` repository and
prints its public `https://images.geler.org/...` URL. Git credentials must already
be available on the machine.
